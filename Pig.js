class Pig extends BaseClass {
    constructor(x, y) {
      super(x,y,50,50);
      this.image=loadImage("sprites/enemy.png");
    }
}

  /*
  INHERITANCE
  Parent - BASE CLASS - takes cares of the needs of all the children
  Children - Inherit all the properties & functions from the parent
           - Can have independent properties & functions too
           - SUB CLASSES

  */
  